header.header
  h1.page__title #{page}
  nav.menu 
    each item in paginas 
      if item === "inicio"
        a.menu__item(target="_blank" href="/") #{item}
      else
        a.menu__item(target="_blank" href=item + ".html" ) #{item}
    